import {StyleSheet} from 'react-native';

export default Styles = StyleSheet.create({
    centredHome:{
        flex:1,
        justifyContent:'center'
    },
    centredText:{
        textAlign:'center',
        color:'black'
    }
})